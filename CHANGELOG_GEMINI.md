# تغییرات: استفاده از Google Gemini به جای Claude

## 🎉 خلاصه تغییرات

سیستم تحلیل هوش مصنوعی از **Claude API** به **Google Gemini API** تغییر کرده است.

### چرا Gemini؟

✅ **کاملاً رایگان** - بدون نیاز به پرداخت
✅ **کوتای بالا** - 1,500 درخواست در روز (رایگان)
✅ **سریع** - مدل gemini-1.5-flash بسیار سریع است
✅ **پشتیبانی از فارسی** - عالی برای تحلیل متن فارسی
✅ **توکن بالا** - تا 1 میلیون توکن ورودی

## 📝 فایل‌های تغییر یافته

### 1. Dependencies

**قبل:**
```json
"@anthropic-ai/sdk": "^0.32.1"
```

**بعد:**
```json
"@google/generative-ai": "^0.21.0"
```

### 2. AI Analyzer Package

**فایل‌های جدید:**
- `packages/ai-analyzer/src/GeminiAnalyzer.ts` ✅ جدید

**فایل‌های آپدیت شده:**
- `packages/ai-analyzer/src/index.ts`
- `packages/ai-analyzer/package.json`

### 3. API Configuration

**فایل‌های آپدیت شده:**
- `apps/api/src/config/env.ts`
- `apps/api/src/workers/analyzerWorker.ts`

**تغییر متغیر محیطی:**
```bash
# قبل
ANTHROPIC_API_KEY=...

# بعد
GEMINI_API_KEY=...
```

### 4. Shared Constants

**فایل آپدیت شده:**
- `packages/shared/src/constants.ts`

**تغییر:**
```typescript
// قبل
export const CLAUDE_CONFIG = {
  model: 'claude-3-5-sonnet-20241022',
  maxTokens: 4096,
  ...
}

// بعد
export const GEMINI_CONFIG = {
  model: 'gemini-1.5-flash',
  maxOutputTokens: 8192,
  ...
}
```

### 5. Environment Files

**فایل‌های آپدیت شده:**
- `.env.example`

### 6. Documentation

**فایل‌های آپدیت شده:**
- `README.md`
- `QUICKSTART.md`

**فایل‌های جدید:**
- `GEMINI_SETUP.md` ✅ راهنمای کامل استفاده از Gemini

## 🚀 مراحل به‌روزرسانی

### برای کاربران جدید:

کافیه مراحل عادی setup رو دنبال کنید:

1. نصب dependencies:
```bash
pnpm install
```

2. دریافت Gemini API Key:
```
https://aistudio.google.com/app/apikey
```

3. تنظیم `.env`:
```bash
GEMINI_API_KEY=your_api_key_here
```

4. اجرا:
```bash
pnpm build
pnpm --filter @media-intelligence/api dev
```

### برای کاربران فعلی (اگر قبلاً Claude داشتید):

1. آپدیت dependencies:
```bash
pnpm install
```

2. آپدیت فایل `.env`:
```bash
# حذف این خط:
# ANTHROPIC_API_KEY=...

# اضافه کردن این خط:
GEMINI_API_KEY=your_gemini_api_key_here
```

3. بیلد مجدد:
```bash
pnpm build
```

4. اجرا:
```bash
pnpm --filter @media-intelligence/api dev
```

## 📊 مقایسه عملکرد

| ویژگی | Claude Sonnet | Gemini Flash |
|------|--------------|--------------|
| **قیمت** | $3 / 1M توکن | رایگان |
| **کوتای روزانه** | بستگی به پلن | 1,500 درخواست |
| **سرعت** | سریع | خیلی سریع |
| **کیفیت تحلیل** | عالی | خوب |
| **توکن ورودی** | 200K | 1M |
| **توکن خروجی** | 4K | 8K |
| **فارسی** | ✅ عالی | ✅ عالی |

## 🔍 تست تغییرات

بعد از اعمال تغییرات، می‌تونید تست کنید:

### 1. تست Health Check:
```bash
curl http://localhost:3000/health
```

باید لاگ زیر رو ببینید:
```
[INFO] Gemini Analyzer initialized { model: 'gemini-1.5-flash', ... }
```

### 2. تست تحلیل:
یک منبع خبری اضافه کنید و بعد از چند دقیقه، نتایج تحلیل رو چک کنید:

```bash
curl http://localhost:3000/articles/recent?limit=5
```

## 📚 مستندات جدید

راهنمای کامل استفاده از Gemini:
```
GEMINI_SETUP.md
```

## ⚠️ نکات مهم

### 1. محدودیت‌های رایگان
- 1,500 درخواست در روز
- 15 درخواست در دقیقه
- برای سیستم خبری کاملاً کافیه!

### 2. سازگاری
تمام API های قبلی بدون تغییر باقی موندن. فقط موتور تحلیل عوض شده.

### 3. کیفیت تحلیل
کیفیت تحلیل Gemini برای متن فارسی عالیه و تقریباً معادل Claude است.

## 🐛 مشکلات احتمالی

### خطا: GEMINI_API_KEY is required
**راه حل:** API Key رو در فایل `.env` تنظیم کنید

### خطا: Invalid API Key
**راه حل:**
- API Key رو چک کنید
- مطمئن شوید فضای خالی اضافه نداره
- از Google AI Studio دوباره دریافت کنید

### خطا: Rate limit exceeded
**راه حل:**
- concurrency رو کم کنید در `.env`:
```bash
ANALYZER_CONCURRENCY=2
```

## 📞 پشتیبانی

برای راهنمایی بیشتر:
- [GEMINI_SETUP.md](GEMINI_SETUP.md) - راهنمای کامل
- [Google AI Docs](https://ai.google.dev/docs) - مستندات رسمی
- [Gemini API](https://ai.google.dev/pricing) - اطلاعات قیمت‌گذاری

---

## ✨ خلاصه

استفاده از Gemini مزایای زیادی داره:
- هزینه صفر
- کوتای بالا
- عملکرد عالی
- پشتیبانی کامل از فارسی

برای شروع فقط یه API Key رایگان از Google بگیرید و در `.env` قرار بدید! 🚀
